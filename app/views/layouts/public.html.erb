<!DOCTYPE html>
<html lang="en">
<head>
  <title>Social</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <!-- Required meta tags -->
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body class="bg-dark bg-dark-image">
<%= controller_name %>
<div class="container-fluid ">
  <div class="row">
    <div class="col-sm-9 col-md-7 col-lg-5 mx-auto rounded-0">
      <div class="card card-signin my-5 rounded-0">
        <div class="card-header p-1">
          <ul class="nav nav-tabs" id="myTab" role="tablist">
            <li class="nav-item col-sm-6 m-0 pl-0 pr-0 text-center">
              <a class="nav-link <%= 'active bg-secondary text-light' if controller_name == 'sessions' %>" id="sign-in-tab" href="<%= new_user_session_path %>">
                LOGIN
              </a>
            </li>
            <li class="nav-item col-sm-6 m-0 pl-0 pr-0 text-center">
              <a class="nav-link <%= 'active' if controller_name == 'registrations' %>" id="sent_invites-tab" href="<%= new_user_registration_path %>">
                SIGN UP
              </a>
            </li>
          </ul>
        </div>
        <div class="card-body">
          <%= yield %>
        </div>
      </div>
    </div>
  </div>
</div>
</body>
</html>
